---
# driver:
  # name: vagrant

provisioner:
  name: chef_zero
  # You may wish to disable always updating cookbooks in CI or other testing environments.
  # For example:
  #   always_update_cookbooks: <%= !ENV['CI'] %>
  always_update_cookbooks: true

verifier:
  name: inspec

platforms:
  - name: ubuntu-18.04
  # - name: centos-7
    transport:
      hostname: ec2-3-16-38-237.us-east-2.compute.amazonaws.com
      ssh_key: C:\Users\rle0594\Downloads\RHEL_Oct10.pem
      username: ubuntu
      elevated: true


suites:
  - name: default
    run_list:
      - recipe[server_setup::default]
    verifier:
      inspec_tests:
        - test/integration/default
    attributes:
